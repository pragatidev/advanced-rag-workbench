# Semantic chunking can hurt keyword search

A semantic cut groups meaning. An error code and its runbook paragraph may stay together (good) or a long community of "errors in general" may swallow TS-999 (bad for BM25).

Always print BM25 recall next to dense recall when you change the chunker. Recursive often keeps IDs. Semantic may win paraphrase.
