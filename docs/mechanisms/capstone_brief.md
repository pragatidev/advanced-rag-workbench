# What the capstone must prove

On this corpus, with this question file:

1. Run naive versus the stack you kept.
2. Write `runs/naive_vs_hybrid/metrics.json` (or your pair).
3. Write a one-page `decision.md` that cites a metrics row for every keep-or-kill.

Fluency is not a citation.
